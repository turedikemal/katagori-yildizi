/* Kategori Yildizi — compatibility stub.
   Device/page profile ownership moved to admin-surface-settings.js.
   Keeping this file intentionally inert avoids duplicate listeners, repeated renders and profile races. */
(function(){'use strict';document.documentElement.dataset.kyLegacyDeviceOverrides='disabled';})();
