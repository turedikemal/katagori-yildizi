/* Kategori Yildizi — compatibility stub.
   Template palette synchronization is handled by the unified editor runtime. */
(function(){'use strict';document.documentElement.dataset.kyLegacyTemplateColorSync='disabled';})();
